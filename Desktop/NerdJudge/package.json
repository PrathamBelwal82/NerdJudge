{
  "name": "nerdjudge",
  "private": true,
  "description": "NerdJudge — run backend + frontend together",
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,magenta \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
